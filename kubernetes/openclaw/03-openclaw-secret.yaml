apiVersion: v1
kind: Secret
metadata:
  name: openclaw-secrets
  namespace: ai-ops
type: Opaque
stringData:
  OPENCLAW_GATEWAY_TOKEN: "${OPENCLAW_GATEWAY_TOKEN}"
  TLF: "${TLF}"
  OPENAI_API_KEY: "${OPENAI_API_KEY}"